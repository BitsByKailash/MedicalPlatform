<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Online Doctors</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0f0f0f, #1c1c1c);
      color: #f0f0f0;
      min-height: 100vh;
    }

    .container {
      padding-top: 5rem;
    }

    .card.shadow-lg {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      box-shadow: 0 8px 40px rgba(0, 0, 0, 0.4);
      overflow: hidden;
    }

    .card-header.bg-primary {
      background: linear-gradient(135deg, #6a11cb, #2575fc) !important;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      padding: 2rem;
    }

    .card-header h2 {
      font-weight: 600;
      font-size: 2rem;
    }

    .card-body {
      padding: 2rem;
    }

    .card-body p.lead {
      font-size: 1.2rem;
      color: #cccccc;
    }

    .text-danger {
      color: #ff6b6b !important;
    }

    .card.shadow-sm {
      background: rgba(255, 255, 255, 0.08);
      border-radius: 16px;
      transition: all 0.3s ease-in-out;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
    }

    .card.shadow-sm:hover {
      transform: scale(1.03);
      box-shadow: 0 10px 25px rgba(0, 200, 255, 0.3);
      border: 1px solid rgba(0, 200, 255, 0.2);
    }

    .card-img-top {
      border-top-left-radius: 16px;
      border-top-right-radius: 16px;
    }

    .card-title {
      font-size: 1.3rem;
      color: #ffffff;
    }

    .card-text.text-secondary {
      color: #bbbbbb !important;
    }

    .card-text.text-success {
      color: #4caf50 !important;
    }

    .card-text a {
      color: #90caf9;
      text-decoration: none;
    }

    .btn-primary {
      background: linear-gradient(135deg, #00c6ff, #0072ff);
      border: none;
      font-weight: 600;
      border-radius: 12px;
      padding: 10px 20px;
      transition: 0.3s ease;
    }

    .btn-primary:hover {
      background: linear-gradient(135deg, #0072ff, #00c6ff);
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(0, 123, 255, 0.3);
    }
  </style>
</head>
<body>

  <div class="container py-5">
    <div class="card shadow-lg border-0 rounded-4">
      <div class="card-header bg-primary text-white">
        <h2 class="mb-0">Doctors Currently Online</h2>
      </div>
      <div class="card-body p-4">
        <p class="lead">Find the best doctors available for consultation.</p>

        <% if (doctor.length === 0) { %>
          <p class="text-danger">No doctors are currently online.</p>
        <% } else { %>
          <div class="row">
            <% doctor.forEach((doctor) => { %>
              <div class="col-md-4 mb-4">
                <div class="card shadow-sm">
                  <img src="<%= doctor.image || './doctor_placeholder..png' %>" class="card-img-top" alt="Doctor Image" style="height: 200px; object-fit: cover;">
                  <div class="card-body">
                    <h5 class="card-title"><%= doctor.doctorname %></h5>
                    <p class="card-text text-secondary">Specialization: General Physician</p>
                    <p class="card-text text-success">Status: Online</p>
                    <div class="d-flex justify-content-between align-items-center flex-wrap">
                      <p class="card-text mb-2"><strong>Contact:</strong> <a href="tel:<%= doctor.doctorphone %>"><%= doctor.doctorphone %></a></p>
                      <button class="btn btn-primary btn-lg mt-2" data-doctor-id="<%= doctor.doctorid %>">Book Appointment</button>
                    </div>
                  </div>
                </div>
              </div>
            <% }) %>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="./match_maker.js"></script>
</body>
</html>
